<!DOCTYPE html>
<html lang="en">
	<head>
		<%- include("./partial/head") %>
	</head>

	<body>
		<div class="container admin-form">
			<div class="row h-100 align-items-center justify-content-center">
				<div class="col-10 col-md-6 col-lg-5 p-4 admin-form__column">
					<% if (headTitle === "Admin Register") { %> <%- include("./partial/adminRegister") %> <% } else if (headTitle === "Admin Login") {
					%> <%- include("./partial/adminLogin") %> <% } else if (headTitle === "Admin Create") { %> <%- include("./partial/adminCreate") %>
					<% } else if (headTitle === "Admin Edit") { %> <%- include("./partial/adminEdit") %> <% } %>
				</div>
			</div>
		</div>

		<%- include('./partial/footer') %>
	</body>
</html>
